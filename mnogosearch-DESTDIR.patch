diff -Nur mnogosearch-3.2.8/Makefile.am mnogosearch-3.2.8-destdir/Makefile.am
--- mnogosearch-3.2.8/Makefile.am	Fri Oct 11 12:23:05 2002
+++ mnogosearch-3.2.8-destdir/Makefile.am	Fri Feb  7 13:08:09 2003
@@ -5,21 +5,21 @@
 # copy dirs w/out Makefile.am into dist
 dist-hook:
 #	mkdir $(distdir)/include
-	cp -r $(srcdir)/include/ $(distdir)/include/
+	cp -r $(srcdir)/include/ $(DESTDIR)$(distdir)/include/
 	rm -f $(distdir)/include/udm_config.h $(distdir)/include/stamp.h 
-	mkdir $(distdir)/create
-	cp -r $(srcdir)/create/ $(distdir)/create
-	mkdir $(distdir)/misc
-	cp -r $(srcdir)/misc/ $(distdir)/misc
+	mkdir $(DESTDIR)$(distdir)/create
+	cp -r $(srcdir)/create/ $(DESTDIR)$(distdir)/create
+	mkdir $(DESTDIR)$(distdir)/misc
+	cp -r $(srcdir)/misc/ $(DESTDIR)$(distdir)/misc
 
 install-data-local:
-	$(mkinstalldirs) $(localstatedir)
-	$(mkinstalldirs) $(localstatedir)/raw/
-	$(mkinstalldirs) $(localstatedir)/splitter/
-	$(mkinstalldirs) $(localstatedir)/tree/
-	$(mkinstalldirs) $(localstatedir)/cache/
-	$(mkinstalldirs) $(localstatedir)/store/
-	chmod 777 $(localstatedir)/cache/
+	$(mkinstalldirs) $(DESTDIR)$(localstatedir)
+	$(mkinstalldirs) $(DESTDIR)$(localstatedir)/raw/
+	$(mkinstalldirs) $(DESTDIR)$(localstatedir)/splitter/
+	$(mkinstalldirs) $(DESTDIR)$(localstatedir)/tree/
+	$(mkinstalldirs) $(DESTDIR)$(localstatedir)/cache/
+	$(mkinstalldirs) $(DESTDIR)$(localstatedir)/store/
+	chmod 777 $(DESTDIR)$(localstatedir)/cache/
 
 uninstall-local:
 	-rmdir $(localstatedir)/raw/
